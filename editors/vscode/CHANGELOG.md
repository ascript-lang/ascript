# Changelog

## 0.6.0

- Initial release: LSP client (`ascript lsp`), TextMate highlighting, language configuration,
  server discovery (PATH / `ascript.server.path` / optional checksum-verified download), restart
  and version commands.
