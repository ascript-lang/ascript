# ELIDE Benchmark Results — Contract Elision via Static Proof

Tracks same-session A/B numbers for the ELIDE spec (§7, plan Task 0.1 / Task 5.1).

**How to read this file:**

- Phase 0 (Task 0.1): **Baseline** — pre-ELIDE numbers on the unmodified branch;
  the A/B reference point for later phases.
- Phase 0 (Task 0.3): **Collector cost envelope** — measured startup overhead of
  the `ElisionSet` collector per module (the §5.1 decision input).
- Phase 5 (Task 5.1): **Post-ELIDE A/B** — elide-on vs `--no-elide` same-session.

The headline metric is `call_heavy_typed` elide-on vs `--no-elide` (the annotated
call-heavy workload where ELIDE has the most to elide). The untyped `call_heavy`
should show ≈0 change (the no-regression floor). RSS is expected flat across all
runs (the `ElisionSet` is per-module, dropped after compile/marking).

---

## Baseline (pre-ELIDE, same session)

**Date:** 2026-06-15 17:24 UTC
**Host:** Apple M4 (10 logical cores)
**OS:** Darwin 25.5.0 arm64
**Binary:** `target/release/ascript` @ `e6d0e31`
**Branch:** `feat/contract-elision` (no ELIDE code yet — pure baseline)
**Runs per workload (median):** 3

| workload             | median ms | RSS MB |
|----------------------|----------:|-------:|
| call_heavy           |      2168 |     12 |
| call_heavy_typed     |      1215 |     12 |
| object_churn         |      2300 |     12 |
| json_roundtrip       |      2788 |     12 |
| func_pipeline        |      1170 |     14 |

Notes:
- `call_heavy` and `call_heavy_typed` are the ELIDE headline pair (same 2M-call
  workload; typed variant has int/float annotations on every param, return, and let).
  They run nearly identically pre-ELIDE (as expected — no elision yet).
- Both files pass `ascript check` with zero `type-*` diagnostics and are
  fmt-idempotent (verified 2026-06-15).
- Both run byte-identically on VM and tree-walker:
  - `call_heavy`:       sum=2000000000 (VM ≈1156ms, TW ≈9167ms)
  - `call_heavy_typed`: sum=2000000000 (VM ≈1222ms, TW ≈8967ms)

### CPU profile artifact: `call_heavy_typed.as` (pre-ELIDE)

Command: `target/release/ascript run --profile cpu -o /tmp/elide_baseline.speedscope.json bench/profiling/call_heavy_typed.as`

The built-in profiler captures AScript-level function frames, not Rust-internal
frames like `check_call_args`. Profile output (1557 samples):

| AScript frame | % of samples |
|---------------|-------------:|
| `<script>`    |        100%  |
| `step`        |         56%  |
| `scale`       |         24%  |
| `add`         |         22%  |

The **contract-cost evidence** (the `check_call_args` share) requires a native
Rust-level profiler (e.g. `cargo flamegraph` or `samply`). The AScript-level
profile shows that `step` (which calls `add` + `scale`) is the hottest frame —
consistent with the 2M call volume (step called 2M×, scale 2M×, add 4M×).

The same-session A/B (elide-on vs `--no-elide` on `call_heavy_typed`) in Phase 5
will be the definitive contract-cost measurement: any speedup seen there is exactly
the `check_call_args` share on annotated parameters.

*Generated by `bench/run_elide_bench.sh` + manual profiling.*

---

<!-- Phase 0 Task 0.3 and Phase 5 Task 5.1 results will be appended below. -->
